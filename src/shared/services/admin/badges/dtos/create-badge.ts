export interface CreateBadge {
    name: string,
    image: string,
    points: number,
    section_id: number,
    count_of_badges: number,
    is_from_section: boolean,
    is_grand_master: boolean,
}